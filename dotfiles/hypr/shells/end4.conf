# end-4 (illogical-impulse) — upstream ships a lua-based hyprland config that
# assumes it owns ~/.config/hypr, so it gets its own standalone config in
# shells/end4/ (independent of the other shells).
source = ~/.config/hypr/shells/end4/hyprland.conf
